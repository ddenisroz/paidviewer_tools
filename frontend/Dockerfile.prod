# РњРЅРѕРіРѕСЌС‚Р°РїРЅР°СЏ СЃР±РѕСЂРєР° РґР»СЏ РїСЂРѕРґР°РєС€РµРЅР°
FROM node:18-alpine as builder

WORKDIR /app

# РљРѕРїРёСЂСѓРµРј package files
COPY package*.json ./
RUN npm ci

# РљРѕРїРёСЂСѓРµРј РёСЃС…РѕРґРЅРёРєРё Рё СЃРѕР±РёСЂР°РµРј
COPY . .
RUN npm run build

# Production stage СЃ Nginx
FROM nginx:alpine

# РљРѕРїРёСЂСѓРµРј СЃРѕР±СЂР°РЅРЅС‹Рµ С„Р°Р№Р»С‹
COPY --from=builder /app/dist /usr/share/nginx/html

# РљРѕРїРёСЂСѓРµРј РєРѕРЅС„РёРіСѓСЂР°С†РёСЋ nginx
COPY nginx.conf /etc/nginx/nginx.conf

# Р­РєСЃРїРѕРЅРёСЂСѓРµРј РїРѕСЂС‚С‹
EXPOSE 80 443

CMD ["nginx", "-g", "daemon off;"]
