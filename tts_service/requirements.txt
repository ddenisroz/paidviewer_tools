fastapi
uvicorn
python-dotenv
sqlalchemy
aiohttp

# ============================================================================
# PyTorch с CUDA 12.4 (установить ПЕРВЫМ!)
# ============================================================================
# ВАЖНО: Установите PyTorch с CUDA перед остальными зависимостями:
# pip install torch==2.4.0+cu124 torchaudio==2.4.0+cu124 torchvision==0.19.0+cu124 --extra-index-url https://download.pytorch.org/whl/cu124
#
# Для CPU версии (без GPU):
# pip install torch==2.4.0 torchaudio==2.4.0 torchvision==0.19.0
#
# torch>=2.4.0  # Устанавливается отдельно с CUDA
# torchaudio>=2.4.0  # Устанавливается отдельно с CUDA
# torchvision>=0.19.0  # Устанавливается отдельно с CUDA

# ============================================================================
# F5-TTS и аудио обработка
# ============================================================================
f5-tts>=0.1.0
soundfile>=0.12.1
librosa>=0.10.1
pydub>=0.25.1
numpy>=1.24.0,<2.0.0
faster-whisper>=0.10.0
ruaccent>=2.0.0

# redis - removed, using database instead
# aioredis - removed, using database instead
