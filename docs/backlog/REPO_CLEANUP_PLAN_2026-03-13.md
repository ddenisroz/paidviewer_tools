# Repo Cleanup Plan (Historical)

This file is preserved only as a historical note after the active docs cleanup on 2026-03-13.

What was folded into active docs:

- repo hygiene rules -> `docs/REPO_STRUCTURE.md`
- workspace cleanup commands -> `scripts/prepare-release.ps1`
- current handoff state -> `docs/STATUS_TRACKER.md`

What was removed from active surface:

- plan-style guidance that duplicated `REPO_STRUCTURE.md`
- stale cleanup notes that no longer matched the current repo layout
- references to legacy scripts and snapshot docs that were moved to `docs/backlog/`

Use this file only as historical context, not as the current source of truth.
